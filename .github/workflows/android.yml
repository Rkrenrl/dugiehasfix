name: dugiehasfix

on:
  workflow_dispatch:
  push:
    branches:
    - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle
    
      - name: Grant Gradle permission
        run: chmod +x ./gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug -Pandroid.injected.testOnly=false

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Termux-SecureFolder-debug
          path: |
            **/build/outputs/apk/**/*.apk
            if-no-files-found: error

       
   
